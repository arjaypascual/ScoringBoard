name: registration
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  mysql_client: ^0.0.27
  cupertino_icons: ^1.0.8
  pdf: ^3.11.0        # ← keep only this one
  printing: ^5.12.0
  file_picker: ^8.1.2
  archive: ^3.4.0
  excel: ^4.0.3
  open_file: ^3.3.2
  path_provider: ^2.1.3
  intl: ^0.19.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    